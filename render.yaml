services:
  - type: web
    name: redstone-backend-1
    env: node
    plan: free
    buildCommand: echo "Build complete"
    startCommand: node server.js
    healthCheckPath: /api/health
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3000
